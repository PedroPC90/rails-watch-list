<h1>New Bookmark</h1>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.submit "Submit new bookmark", class: "btn btn-primary" %>
  <%= f.association :movie %>
<% end %>
